# Frontend - OrnoPlante Web App

This is the Next.js frontend for the OrnoPlante application.

## Getting Started

First, ensure the backend server is running. Then, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Environment Variables

Create a `.env.local` file and add the following variables:

```
NEXT_PUBLIC_API="http://localhost:8080"
```

## Running Tests

To run the Jest test suite:

```bash
npm test
```

To run Cypress end-to-end tests:

```bash
npm run cypress:open
```

## Learn More

To learn more about Next.js, take a look at the [Next.js Documentation](https://nextjs.org/docs).